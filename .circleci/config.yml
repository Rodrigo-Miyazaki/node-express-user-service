version: 2.1
orbs:
  node: circleci/node@2.0.2
jobs:
  build:
    docker:
      - image: circleci/mongo:4.0.5-node
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: npm test      
workflows:
  build-and-test:
    jobs:
      - build